// PixiJS renderer — plug-in replacement for render.js
// Implements the same draw(canvas, ctx, state) signature
// TODO: replace stub with full PixiJS implementation

export function draw(canvas, _ctx, state) {
    // PixiJS takes over rendering from here
    // ctx is unused — PixiJS manages its own WebGL context
    console.warn("renderPixi: stub not yet implemented");
  }